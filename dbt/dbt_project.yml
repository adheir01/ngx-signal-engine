name: ngx_signal_engine
version: "1.0.0"
config-version: 2

profile: ngx_signal_engine

model-paths: ["models"]
test-paths: ["tests"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  ngx_signal_engine:
    staging:
      +materialized: view
    marts:
      +materialized: table
