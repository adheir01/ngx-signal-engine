POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_DB=ngx_signals
POSTGRES_USER=ngx_user
POSTGRES_PASSWORD=changeme

GEMINI_API_KEY=your_key_here

# Portfolio config
# Number of top BUY positions to include in daily portfolio
PORTFOLIO_TOP_N=5

# Gemini — minimum signal strength to trigger LLM explanation
# Keeps usage within free tier (20 requests/day)
GEMINI_MIN_STRENGTH=30

# Scheduler — pipeline runs daily at 16:00 WAT (15:00 UTC)
# Change hour/minute in ingestion/scheduler.py if needed